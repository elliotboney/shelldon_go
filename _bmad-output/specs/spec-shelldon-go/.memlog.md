---
topic: shelldon — Go rewrite of the chat-first E-Ink AI pet on Pi Zero 2W (same product as Python v2, new Go architecture)
updated: 2026-06-20T07:52
---

- (note) Derive from Python v2 SPEC (spec-openclawgotchi-v2) — same PRODUCT, capabilities CAP-1..CAP-11 carry with stable IDs/intents/success. Deltas: (a) Python-specific constraints refreshed to Go; (b) adopt the NEW Go ARCHITECTURE-SPINE as companion; (c) minor HOW-leak cleanup in capability intents.
- (capability) CAP-1..CAP-11 carried verbatim from Python v2 spec (chat-turn brain+face, resident reflexes, optional physical sensing, proactive action, capability broker, hybrid sqlite+markdown memory, one plugin model, LLM fallback, pluggable transport, autonomous scheduler, self-improving dream cycle). Stable IDs preserved.
- (decision) Why updated: this is now the GO rewrite — the clean-spine rebuild moves from Python multi-process to one supervised Go process (per ARCHITECTURE-SPINE AD-1), keeping the same chat-first transport-agnostic pet vision.
- (constraint) CONSTRAINT CHANGED (Python→Go): drop 'Python cold-start 0.3-1s turn floor' and 'fork warm-start required' — Go has no cheap fork; the worker is goroutine-per-turn behind a swappable isolation seam (Monolith+ now, Privsep-lite subprocess at M3). Replaces the fork-server constraint.
- (constraint) CONSTRAINT CHANGED (Python→Go): 'per-turn worker memory reclaimed, nothing accumulates' is now satisfied by Go GC + GOMEMLIMIT≈280MiB, not process death. The v1 OOM failure is designed out by bounded GC, not by forking.
- (constraint) CONSTRAINT ADDED (Go): single static binary, CGO_ENABLED=0, GOARCH=arm64 — pure-Go deps only (modernc.org/sqlite for FTS5, periph.io) so cross-compile stays trivial. This bends every dependency choice.
- (constraint) CONSTRAINT REFRAMED (Go): LLM-free core enforced by depguard + internal/ packages (was Python import-linter); broker provider SDKs live behind broker/internal/. Mechanically build-failing.
- (constraint) CONSTRAINT ADDED (Go security staging): the secret vault does not EXIST until the worker is across a process/uid wall (Privsep-lite, M3) — no goroutine-worker can ever read a populated vault (ARCHITECTURE-SPINE AD-3).
- (decision) Adopt companion: the NEW Go ARCHITECTURE-SPINE.md (architecture-shelldon_go-2026-06-19) replaces the Python spine as the architecture companion. Diagrams + AD-1..AD-17 live there, not in SPEC.
- (question) OPEN: threat-model confirmation that the worker is untrusted — settled in principle (AD-3), but the explicit confirmation + vault-isolation property test is the M3 gate. Carry as open question, not M0 blocker.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6/8): PASS. All 11 capabilities have intent+success; constraints all bend design; ≥1 non-goal (many); success signal concrete+demonstrable; CAP IDs stable/unique; prose lean. CAP-6/CAP-11 carry some structural detail (sqlite/FTS5/memory-op) — load-bearing product contract, kept as in the source spec.
- (event) Self-validate Pass 2 (preservation): PASS. Every load-bearing claim from the Python v2 SPEC landed — 11 capabilities verbatim, all constraints carried (Python-specifics refreshed to Go), all non-goals carried + 2 added (no dynamic plugin loading, gokrazy deferred), Why+Success updated for the Go rewrite. Dropped only the superseded Python architecture-spine companion (replaced by the adopted Go spine). No silent drops.
- (event) spec finalized
